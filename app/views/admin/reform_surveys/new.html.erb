<% model_class = ReformSurvey %>
<% page_title t('shared.actions.with_obj.new',
                obj: t('activerecord.models.reform_survey', count: 1)) %>

<div class='actions-container'>
  <%= render partial: 'shared/new_actions',
        locals: {
          hide_object_name: true,
          object: Verdict.new,
          index_path: admin_verdicts_path(v: @verdict.slug)
                }
  %>
</div>

<%= render partial: 'form' %>
