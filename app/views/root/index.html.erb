<div>

  <div class='topImageContainer mod-homepage'>
    <%= render partial: 'application/navbar_header' %>

    <section>

      <% if @home_page_about %>

        <div>

          <h2>
            <%= @home_page_about.title %>
          </h2>

          <%= simple_format_no_tags(@home_page_about.content) %>

        </div>

      <% end %>

      <% if @quarter %>

        <div>

          <h2>
            <%= t('.header.standing', time: @quarter.time_period) %>
          </h2>

          <ul>

            <li>
              <%= simple_format_no_tags(@quarter.summary_good) %>
            </li>

            <li>
              <%= simple_format_no_tags(@quarter.summary_bad) %>
            </li>

          </ul>

          <div>

            <%= link_to t('.report_link'),
                        download_data_and_reports_path(
                          type: 'report',
                          quarter: @quarter.slug),
                        method: :post %>

          </div>

          <div>
            <%= @quarter.expert_survey.overall_score %>
            <%= generate_change_icon(@quarter.expert_survey.overall_change) %>
          </div>

        </div>

      <% end %>

    </section>

  </div>

  <main class='mainContent'>

    <div class='contentSidebarContainer'>

      <div class='contentSidebarContainer-content'>

        <section class='contentSection'>

          <header class='contentSection-header'>

            <h2 class='heading mod-smaller-text'>

              <div class='heading-iconTextContainer'>

                <%= inline_svg('experts.svg', class: 'heading-icon') %>

                <div class='heading-text'>
                  <%= t('.header.experts') %>
                </div>

              </div>

            </h2>

          </header>

          <div class='contentSection-body'>

            <div class='homePage-expertHistoryChart js-become-highchart' data-chart-type='rating-history-time-series' data-id='expert-history'></div>

            <% if @quarter.expert_survey.summary %>

              <div>
                <%= simple_format_no_tags(@quarter.expert_survey.summary) %>
              </div>

            <% end %>

          </div>

        </section>

      </div>

      <div class='contentSidebarContainer-sidebar'>

        <section class='contentSection'>

          <header class='contentSection-header'>

            <h2 class='heading mod-smaller-text'>

              <div class='heading-iconTextContainer'>

                <%= inline_svg('government.svg', class: 'heading-icon') %>

                <div class='heading-text'>
                  <%= t('.header.government') %>
                </div>

              </div>

            </h2>

          </header>

          <div class='contentSection-body'>

            <% @reforms.each do |reform| %>

              <div data-id="<%= reform.slug %>">

                <%= reform.name %>

                <div class='homePage-reforms-item-chart js-become-highchart' data-chart-type='mini-time-series' data-id='reform-<%= reform.slug %>'></div>

              </div>

            <% end %>

          </div>

        </section>

      </div>

    </div>

    <% if @external_indicators.present? %>

      <section class='contentSection'>

        <header class='contentSection-header'>

          <h2 class='heading'>
            <%= t('.header.external_indicators') %>
          </h2>

        </header>

        <div class='contentSection-body'>

          <% @external_indicators.each do |ext_ind| %>

            <h3>
              <%= ext_ind.title %>
            </h3>

            <h4>
              <%= ext_ind.subtitle %>
            </h4>

            <div>insert chart here </div>

          <% end %>

        </div>

      </section>

    <% end %>

  </main>

</div>
